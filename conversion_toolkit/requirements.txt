# JAX GPU Requirements for CUDA 12.4 (RTX Titan)
# Compatible with PyTorch 2.5.1+cu124, Python 3.10.19, NumPy 2.2.6, CuDNN 9.1
# Tested successfully on NVIDIA TITAN RTX with driver 580.95.05

# Core JAX packages with CUDA 12 support
# These will automatically install jax-cuda12-plugin and jax-cuda12-pjrt
# Note: Using JAX 0.4.30 as it's compatible with CuDNN 9.1 (newer versions require CuDNN 9.8+)
jax[cuda12]==0.4.30
jaxlib==0.4.30

# Core dependencies (auto-installed with JAX)
ml-dtypes>=0.5.0
scipy>=1.15.0
opt-einsum>=3.4.0

# CUDA dependencies (auto-installed)
# nvidia-cuda-nvcc-cu12 will be installed automatically

# Neural network libraries - fully tested and working
flax==0.8.2
optax>=0.2.0
orbax-checkpoint>=0.6.0
einops>=0.8.0
chex>=0.1.86

# Installation instructions:
# source ~/caco/bin/activate
# uv pip install --python ~/caco/bin/python -r requirements_jax_gpu.txt

# Verified working test:
# - JAX GPU computation on cuda(id=0)
# - Flax neural network training with gradients
# - PyTorch GPU coexistence without conflicts
